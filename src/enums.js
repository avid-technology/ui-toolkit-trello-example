/**
 * Copyright 2016 by Avid Technology, Inc.
 */

import {createEnum} from './helpers/enum.js';

export const TrelloViewMode = createEnum({
    SIGN_IN: null,
    BOARDS: null,
    LISTS: null,
    CARD: null,
});
